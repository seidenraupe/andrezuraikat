{
  "name": "andrezuraikat",
  "version": "0.1.0",
  "private": true,
  "description": "Website von André Zuraikat — statische HTML-Site mit Vite",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 5173",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm test"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
